{
  "name": "dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS multi-tenant de inteligência para tráfego pago e gestão financeira (codinome TrafficIntel)",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "fastify": "4.29.1"
    }
  }
}
